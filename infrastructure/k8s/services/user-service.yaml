apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: gruenfahrt
  labels:
    app: gruenfahrt
    service: user-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: gruenfahrt
      service: user-service
  template:
    metadata:
      labels:
        app: gruenfahrt
        service: user-service
        version: v1
    annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
    spec:
      containers:
      - name: user-service
        image: gruenfahrt/user-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
          protocol: TCP
          name: http
        env:
        - name: PORT
          value: "8081"
        - name: SERVICE_NAME
          value: "user-service"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgresql-app-secrets
              key: user-db-url
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: gruenfahrt-shared-config
              key: REDIS_URL
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: jwt-secret-key
        resources:
          requests:
            cpu: "150m"
            memory: "256Mi"
          limits:
            cpu: "300m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: gruenfahrt
  labels:
    app: gruenfahrt
    service: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: gruenfahrt
    service: user-service